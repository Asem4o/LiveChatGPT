{% extends 'bar.html.twig' %}

{% block body %}
    <h1>Admin Profile</h1>

    <h2>Welcome, {{ user.username }}</h2>

    <h3>All Users</h3>
    <ul>
        {% for user in users %}
            <li>
                <a href="{{ path('admin_user_notes', {'id': user.id}) }}">{{ user.username }}</a>
            </li>
        {% endfor %}
    </ul>
{% endblock %}
